apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service-external
  namespace: data
spec:
  type: LoadBalancer
  selector:
    app: rabbitmq
  ports:
    - name: rabbitmq-port-management
      port: 15672
      targetPort: 15672
