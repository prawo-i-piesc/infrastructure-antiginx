apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  namespace: data
spec:
  type: ClusterIP
  selector:
    app: rabbitmq
  ports:
    - name: rabbitmq-port-amqp
      port: 5672
      targetPort: 5672
