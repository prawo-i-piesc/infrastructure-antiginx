apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: data
spec:
  type: LoadBalancer
  selector:
    app: pgadmin
  ports:
    - name: pgadmin-port
      protocol: TCP
      port: 33000
      targetPort: 80
