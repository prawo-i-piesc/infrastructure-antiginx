apiVersion: v1
kind: Service
metadata:
  name: dvwa-service
  namespace: scan-targets
spec:
  type: ClusterIP
  selector:
    app: dvwa
  ports:
    - name: dvwa-port
      port: 80
      targetPort: 80
